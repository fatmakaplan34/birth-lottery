# Live REST Countries data is used when an API key is configured.
ENVIRONMENT=development
COUNTRY_DATA_MODE=live
RC_API_KEY=your_rest_countries_api_key

# If the key or service is unavailable, the app automatically uses its
# bundled 252-country fallback so the interface does not become unusable.
